# Wildlore — digital field guide PWA (live GBIF/OBIS data, offline-capable).
Run locally: cd into folder, `python3 -m http.server 8080`, open http://localhost:8080
Photo identification works on-device (TensorFlow.js MobileNet, no key). See DEPLOY.md to publish.
